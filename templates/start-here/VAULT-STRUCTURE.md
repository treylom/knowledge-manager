---
type: vault-structure
schema_version: vault-structure-v1
generated_by: km-setup
vault: "{{VAULT_NAME}}"
updated: {{DATE}}
tags: [vault-structure, vault-meta]
---

# 볼트 구조 — {{VAULT_NAME}}

> 폴더가 무엇을 담고, 어떤 번호 규칙을 따르고, 누가 쓰는지의 정본입니다. 폴더를 새로 만들거나 옮기거나 지우면 **이 문서를 같이 고칩니다**. 다른 문서와 어긋나면 이 문서가 우선합니다.
> 현관은 [[START-HERE]], 주제 허브 지도는 [[MOC-Map]].

## 1. 폴더 트리 (깊이 2)

```
{{TREE}}
```

## 2. 번호 접두 규칙

{{PREFIX_TABLE}}

규칙 세 줄:
1. **같은 접두 번호에는 폴더 하나만** 둡니다(예: `020-` 가 둘이면 안 됩니다).
2. **번호 없는 최상위 폴더는 만들지 않습니다**(부속 이미지 같은 예외는 표에 명시).
3. **비어 있는 접두**도 표에 남깁니다 — 다음에 무엇이 들어갈 자리인지 보이게.

## 3. 권한 경계

| 폴더 | 주인 | 쓰기 | 읽기 |
|---|---|---|---|
{{OWNER_ROWS}}

- 혼자 쓰는 볼트면 주인은 전부 「본인」입니다. 협업자·자동화 도구(봇)가 생기면 행을 늘리고, **자기 영역만 쓰고 다른 영역은 읽기만** 합니다.

## 4. 폴더를 바꿀 때

**추가**: 접두 표에서 빈 번호를 고른다 → 폴더를 만든다 → 이 문서 §1·§2 를 고친다.
**이동**: 옮길 폴더의 링크를 먼저 센다(`/km:reform plan`) → 옮긴다(`/km:reform apply`) → 이 문서와 [[MOC-Map]] 을 고친다.
**삭제**: 안에 링크가 걸린 노트가 없는지 확인한다 → 보관 폴더로 옮기거나 지운다 → 이 문서를 고친다.

## 5. 한 줄 원칙

> 자기 영역만 쓰고, 다른 영역은 읽는다. 폴더를 바꾸면 이 문서를 고친다. 어긋나면 이 문서가 이긴다.
